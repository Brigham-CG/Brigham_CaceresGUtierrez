#include <iostream>

using namespace std;

class escitala{
    public:
        int rows = 0, cols = 0;
        string encrypted(string message);
        string decrypted(string messageEncrypted);
};
